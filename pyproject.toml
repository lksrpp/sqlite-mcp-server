[project]
name = "sqlite-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.22.0",
    "faker>=33.0.0",
    "aiosqlite>=0.20.0",
]
